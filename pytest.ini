[pytest]
testpaths = tests
addopts = -q --cov=app --cov=scripts --cov-report=term-missing
markers =
    unit: fast unit tests
    api: FastAPI endpoint tests
    integration: tests that require running services
